---
import { longDate, monthYear } from '../utils/dates';

interface Props {
	date: Date;
	format?: 'long' | 'short';
}

const { date, format = 'long' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{format === 'short' ? monthYear(date) : longDate(date)}
</time>
